# WriteProcess-Dumper
And here's the GitHub description (347 characters):  Windows research tool that hooks WriteProcessMemory and VirtualAllocEx via DLL injection, logging every write and dumping raw buffers to .bin files. Detects PE images, executable memory regions, and shellcode writes. Supports x86 and x64. Built for malware analysis, unpacking, injection research
